name: ai_service
description: Claude API client for tutoring and exercise generation (Phase 3).
  Boundary only for now; `dio` and prompt logic land in the AI milestone.
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.8.0
dependencies:
  core: ^0.1.0
dev_dependencies:
  test: ^1.25.0
  very_good_analysis: ^9.0.0
