name: audio_service
description: just_audio wrapper + TTS for pronunciation (Phase 2). Boundary only
  for now; audio deps land in the audio milestone.
publish_to: none
version: 0.1.0
resolution: workspace
environment:
  sdk: ^3.8.0
  flutter: ">=3.27.0"
dependencies:
  flutter:
    sdk: flutter
  core: ^0.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^9.0.0
